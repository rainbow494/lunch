language: node_js
node_js:
  - "0.12"
sudo: required
before_script:
  - npm install -g gulp
  - npm install -g mocha
  - npm install -g chai
  - npm install -g should
  - npm install -g chai-promise
script: gulp